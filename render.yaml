services:
  - type: web
    name: ryze-app
    env: docker
    plan: free
    autoDeploy: true
    dockerfilePath: Dockerfile
    envVars:
      - key: PORT
        value: 8000
    healthCheckPath: /
    regions:
      - frankfurt
    